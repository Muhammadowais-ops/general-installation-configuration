tmux ------> new alternative for screen

below are the commands

#########

tmux  ----> create new session
tmux a    ---> attach session
tmux list-sesssions   -----> list session that are running
tmux a -t 0 ---->    tmux attach the session
ctrl + b d --> to detach the session
ctrl b % ----> split the windows in multiple tabs
ctrl b q ----> to display the split windows number
ctrl b q 1 -----> give number to jump to that session
Resize Panes in tmux :
Press Ctrl + b, then press : to bring up the command prompt.

Resize down: resize-pane -D (for moving the border down)

Resize up: resize-pane -U (for moving the border up)

Resize left: resize-pane -L (for moving the border left)

Resize right: resize-pane -R (for moving the border right)


screen ----> in old linux

screen ----> create new session
screen -r ----> reattach session
screen -ls  ----> List all running screen sessions:
screen -r session-name ----> to reattach that session
ctrl + a, then d   ---> to detattach the session






vim/vi -----------> editor


0 - jump to the start of the line

$ - jump to the end of the line

gg - go to the first line of the document
G - go to the last line of the document

} - jump to next paragraph (or function/block, when editing code)
{ - jump to previous paragraph (or function/block, when editing code)

e press in the linux to go to next character
b press in the linux to go to end character

dd to deleting
:%d delete all things
dG will delete all lines from where you stand
yy copy
p paste


to copy in Visual press v then select the lines you want to copy
after that then
press y to copy and then p for paste


copy and replace in vim

:%s/word/new-word/g

add multiple comment in vim


:[start line],[end line]s/^/#

how to uncomment multiple lines

:%s/^#/


/word   search from top of file

?word    search from bottom of the file

gg go to top of the file

G go to end of the file

w go to word by word

b go to backword word by word

dd to delete lines

yy to copy 

3yy yo copy 3 lines



vim +8092 catalina.out    open the file from specified line

:linenumber              go to that line

:set number

:set nonumber

:set ignorecase


search for whole word with space
/\<your_exact_word\>

Visual mode 

use v and y for copy


:%s/old/new/g - replace all old with new throughout file
:%s/old/new/gc - replace all old with new throughout file with confirmations
:noh[lsearch] - remove highlighting of search
search for whole word with space
/\<your_exact_word\>


##### aws disk size increase

ext4

sudo growpart /dev/xvda 1
sudo resize2fs /dev/xvda1


 df -h
Filesystem       Size  Used Avail Use% Mounted on
/dev/nvme0n1p1   8.0G  1.6G  6.5G  20% /
/dev/nvme1n1     8.0G   33M  8.0G   1% /data
...

[XFS file system] To extend the file system on each volume, use the xfs_growfs command. In this example, / and /data are the volume mount points shown in the output for df -h.

sudo yum install xfsprogs

[ec2-user ~]$ sudo xfs_growfs -d /
[ec2-user ~]$ sudo xfs_growfs -d /data




###### php common commands after deployment can be run --------------------> also composer is included

php artisan config:clear
php artisan config:cache
php artisan cache:clear


## check laravel framework
php artisan --version

composer install
composer require grpc/grpc


sudo composer require "grpc/grpc": "^1.38.0"
composer update --ignore-platform-reqs











######## linux shortcut commands -----------------------------



Delete everything from the cursor to the end of the line:
Ctrl + k



Delete everything from the cursor to the start of the line:
Ctrl + u


Search your history backward. This is probably the one that I use the most. It is really handy and speeds up my work-flow a lot:
Ctrl + r

Clear the screen, I use this instead of typing the clear command:
Ctrl + l


Stops the output to the screen:
Ctrl + s


Enable the output to the screen in case that previously stopped by Ctrl + s:
Ctrl + q


Throw the current command to background:
Ctrl + z










######### increase swap size in linux this will increase existing swapspace  --------------------------------
it will add existing swap to how much increasing you are


sudo swapoff /swapfile
sudo dd if=/dev/zero of=/swapfile bs=1G count=16 oflag=append conv=notrunc
sudo mkswap /swapfile
sudo swapon /swapfile
swapon --show










########## lvm for orcale disk ------------------------------

sudo dnf install cloud-utils-growpart
sudo growpart /dev/sda 2
sudo pvresize /dev/sda2
sudo lvextend -l +100%FREE /dev/mapper/rootvg-rootlv
sudo xfs_growfs /dev/mapper/rootvg-rootlv








######### mongo dump and commands   ----------------------

how to login into mongo database from cmd line

mongo -u xxxx -p xxxxx --authenticationDatabase admin

---- common commands ----

show dbs

db.createUser({user : 'xxxxxx', pwd : 'xxxxx', roles : ['readWrite']})
db.createUser({user: "xxx",pwd: "xxxx",roles: [ "userAdminAnyDatabase","userAdmin","readWrite","dbAdmin","clusterAdmin","readWriteAnyDatabase","dbAdminAnyDatabase"]});     ---> for clusteradmin


mongodump --host xxxx --port 10255 -u xxxx -p xxxx -d dbname -o dbname.dump --ssl --sslAllowInvalidCertificates --tlsInsecure 
mongodump --host xxxxxx --authenticationDatabase admin -u xxxx -p xxxx --db dbname -o dbname.dump       ---> common dump command



mongodump --authenticationDatabase admin -d dbname -u xxxx -p xxxxx --host xxxx -o dbname.dump --excludeCollection collectionname --excludeCollection collectioname      --> if exclude collection during dump

